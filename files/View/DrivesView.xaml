<UserControl x:Class="files.View.DrivesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:files.View" 
             xmlns:local1="clr-namespace:files.ViewModel"
             mc:Ignorable="d" 
             Height="auto" Width="auto">
    <UserControl.DataContext>
        <local1:DrivesViewModel/>
    </UserControl.DataContext>
    <Grid>
        <ListBox ItemsSource="{Binding Drivers}" SelectedItem="{Binding SelectedItem}"/>
    </Grid>
</UserControl>
